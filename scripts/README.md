## How to use `pdf_to_text.py`

General example:

`python pdf_to_text.py -i /path/to/pdfs/ -o /path/to/txt/`

Requirements:

```
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-rus
pip install PyPDF2
pip install pytesseract pdf2image pillow
```